version: '3.8'

services:

  backend:
    build: ./backend
    container_name: festival_backend
    ports:
      - "5000:5000"

  frontend:
    image: nginx:latest
    container_name: festival_frontend
    ports:
      - "80:80"
    volumes:
      - ./frontend:/usr/share/nginx/html